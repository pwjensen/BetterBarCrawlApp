import 'package:flutter/material.dart';

class BarInfoPage extends StatelessWidget {
  const BarInfoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bar Info Page'),
    );
  }
}
